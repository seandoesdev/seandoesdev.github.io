---
import { termPaths } from '../../../lib/taxonomy';
import TaxonomyTermView from '../../../components/views/TaxonomyTermView.astro';

export const getStaticPaths = () => termPaths('en', 'tags');

const { term, posts } = Astro.props;
---

<TaxonomyTermView kind="tags" term={term} posts={posts} />

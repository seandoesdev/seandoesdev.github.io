---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getLangFromUrl, useTranslations } from '../../i18n/utils';
import { getProjects } from '../../lib/content';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const projects = await getProjects(lang);
---

<BaseLayout title={t('projects.title')}>
  <h1 class="font-serif text-3xl font-semibold tracking-tight">{t('projects.title')}</h1>
  <p class="mt-2 text-stone-500 dark:text-stone-400">{t('projects.note')}</p>
  <ul class="mt-8 divide-y divide-stone-200 dark:divide-stone-800">
    {projects.length === 0 && <li class="py-6 text-stone-500">{t('projects.empty')}</li>}
    {
      projects.map((project) => (
        <li class="py-6">
          <div class="flex items-baseline justify-between gap-4">
            <h2 class="text-lg font-medium text-stone-900 dark:text-stone-100">{project.data.title}</h2>
            <div class="flex shrink-0 gap-4 text-sm text-stone-500">
              {project.data.repo && <a href={project.data.repo} class="transition-colors hover:[color:var(--accent)]">{t('projects.code')}</a>}
              {project.data.demo && <a href={project.data.demo} class="transition-colors hover:[color:var(--accent)]">{t('projects.demo')}</a>}
            </div>
          </div>
          <p class="mt-1.5 leading-relaxed text-stone-500 dark:text-stone-400">{project.data.description}</p>
          {project.data.tech.length > 0 && (
            <div class="mt-3 flex flex-wrap gap-x-3 gap-y-1 text-sm text-stone-400">
              {project.data.tech.map((tech) => (
                <span>{tech}</span>
              ))}
            </div>
          )}
        </li>
      ))
    }
  </ul>
</BaseLayout>

---
import { getLangFromUrl, alternatePaths } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const { ko, en } = alternatePaths(Astro.url);
const target = lang === 'en' ? ko : en;
const label = lang === 'en' ? '한국어' : 'EN';
---

<a
  href={target}
  class="grid h-8 min-w-8 place-items-center rounded-md px-1.5 text-xs font-medium text-stone-500 transition-colors hover:bg-stone-200/60 hover:text-stone-900 dark:text-stone-400 dark:hover:bg-stone-800 dark:hover:text-stone-100"
  aria-label="Switch language"
>
  {label}
</a>
